version: '3.8'

services:
  piston:
    image: ghcr.io/engineer-man/piston:latest
    container_name: mentorhub-piston
    ports:
      - "2000:2000"
    environment:
      - PISTON_BIND_ADDRESS=0.0.0.0:2000
    tmpfs:
      - /tmp
    restart: unless-stopped
